apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: rednest
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: auth-service

