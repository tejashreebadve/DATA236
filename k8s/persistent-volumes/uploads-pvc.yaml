apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traveler-uploads-pvc
  namespace: rednest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Minimal for demo
  storageClassName: gp2-immediate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owner-uploads-pvc
  namespace: rednest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Minimal for demo
  storageClassName: gp2-immediate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: property-uploads-pvc
  namespace: rednest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi  # Minimal for demo
  storageClassName: gp2-immediate

